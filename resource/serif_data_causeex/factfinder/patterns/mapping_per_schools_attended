(per_schools_attended_serif

(wordsets)

(entitylabels (AGENT1))

(reference)

(toplevel

(relation (fulltype ORG-AFF.Student-Alum) (id mapping_1) (score 1.0) (score_group 1)
        (args
                (argument (mention (return (ff_role per_schools_attended_serif)) (min-entitylevel DESC) (acetype ORG)) (role ARG2))
                (argument (mention (return (ff_role AGENT1) (ff_fact_type per_schools_attended_serif)) (entitylabel AGENT1)) (role ARG1))
        )
)
)
)
